
# todo/ dev-only: Local database for development purposes
quarkus.datasource.url=jdbc:postgresql://localhost:5432/db

# todo/ production: Database for production environment (docker-container)
#spring.datasource.url=jdbc:postgresql://prod-db:5432/prod_db

# todo/ dev-only: Local database for development purposes
quarkus.datasource.driver=org.postgresql.Driver

# todo/ production: Database for production environment (docker-container)
#spring.datasource.driver-class-name=org.postgresql.Driver

# /todo/ env: Must be changed to environment variables
spring.datasource.username=user
spring.datasource.password=user

spring.jpa.hibernate.ddl-auto=validate

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true
spring.jpa.properties.hibernate.type=trace
spring.jpa.properties.hibernate.highlight_sql=true

spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration
spring.flyway.schemas=url_shortener

logging.level.org.flywaydb.core=DEBUG
logging.level.org.springframework.security=DEBUG

#
spring.cache.cache-names=authCache
